# 使用说明书 · YouTube 字幕精读稿生成技能

> 技能名：`youtube-subtitle-study-doc`
> 装在哪：`%USERPROFILE%\.workbuddy-ai\skills\youtube-subtitle-study-doc\`
> 适用范围：本机所有项目（不是只在这个文件夹里能用）

---

## 一、这个技能能干什么

给一段 YouTube 自动字幕 txt，它帮你产出一份**中英对照的英语精读 Word 稿**：

- 按语义重新分段（不是机械按时间戳切），目标每段 90–130 词；素材短时可放宽到 80–90
- 修掉 **ASR 听错** 和 **机翻错译**
- 每段英文下面配中文，只留修正后的版本
- 开头一段「全篇大意」
- 文末「重点词汇」：**3 组、20–22 条**，带原文例句；只给 6–8 条难词写用法提示
- 按固定规范排版：字体、字号、缩进、空行、加粗、超链接、页码，一次到位

---

## 二、怎么用

### 最省事：一句话 + 文件

> 把这个字幕整理成精读稿：`%USERPROFILE%\Downloads\xxx.txt`

或者：

> 帮我整理一下这个 YouTube 字幕，做成中英对照的 Word
> （然后把 txt 拖进来）

技能会自动被触发，不用报技能名。

### 会问你什么

**每次都会先问一件事：原视频链接。**

- **你给了链接** → 它直接开工，头部写 3 行（标题 / 原视频链接 / 整理日期）
- **你没给链接** → 它会**先停下来问你**：
  「没看到原视频链接 —— 要提供吗？没有的话我就按无链接版本做（头部只留标题 + 整理日期）」
  - 你说**没有** → 按 2 行头部的版本做，**不会自己去网上翻**
  - 你**补发链接** → 它把 `原视频链接：` 那一行加进去，其余不动

> 🚫 **「频道」「视频时长」已经不写了**（2026-09-24 你改的口径 —— 查这两样太耗时间）。
> 老稿子里留着的不用管，新稿一律不写，也不会再上网查。

除此之外**一般不问你**，除非：

1. **描述和文件对不上** —— 比如你说"改成宋体"，但文件里实际是仿宋。这种它会先确认再动手（吃过一次亏，猜错要重做几百段）。
2. **规范里有多个合理选项** —— 比如字号在第一段里不统一。

### 产出在哪

- 文件名 = **当天日期 + 视频标题**，如 `2026-09-23 The Reality of Change.docx`
  - **标题默认就用你给的那个 txt 的文件名**（去掉 `.txt`）
  - 没给文件（比如直接把文本粘进来）时，它会按英文内容自己总结一个标题
  - 标题跟文档里的大标题一致，一眼知道是哪期
  - 标题里若有 `: / \ ?` 这类 Windows 不让用的符号，会自动换成 `-`
- 默认存到 `E:\EnglishStudy\YouTube英语字幕\`
- 想换目录直接说："存到 D 盘桌面"

---

## 三、成品长什么样（当前规范）

### 结构

**你给了原视频链接时：**

```
The Reality of Change                        ← 居中，黑体 22pt
原视频链接：https://www.youtube.com/watch?v=xxxx     ← 可点超链接
整理日期：2026-09-23
```

**你没给链接时（只留两行）：**

```
The Reality of Change                        ← 居中，黑体 22pt
整理日期：2026-09-24
```

> 开关只有一个：**你有没有给原视频链接**。
> **没给链接时它会先问你一次**（"要提供吗？"），你说没有才按 2 行的版本做 ——
> 它不会自己去网上翻元信息来凑；你后来补发了链接，它会给你加上。
> **「频道」「视频时长」已废弃**，不再写也不再查。

后面都一样：

```
（全篇大意正文，200–250 字，不写小标题）

一、分段精读
[00:00]
I didn't think this would be the reality of change. ……

我从没想过，"改变"的真面目会是这样。……

（空行）

[00:19]
……

二、重点词汇
① 择偶与关系
1. soulmate　n.　灵魂伴侣
例：I don't believe there's such thing as soulmate. — 我不相信有"灵魂伴侣"这回事。

2. compatible　adj.　合得来的、兼容的
例：I need to find a partner who I'm truly compatible with. — 我想找一个真正合得来的人。
提示：搭配介词 with；强调"各方面都合得上"，比 fit 正式。

② 情绪与心态
……

③ 句式与搭配
……
```

> **词表是 3 组、20–22 条**，每条固定 2 行（词条 + 例句）。
> **只有 6–8 条"有坑"的**才多一行 `提示：` —— 废话提示（"搭配介词 with"这种）一律不写。
> 原来的「三、重点句子」整节**已经删掉**（2026-09-24）：那 8 句正文里都有、还带译文，
> 唯一有价值的"看点"现在并进了词条的 `提示：`。

### 字体字号

| 对象 | 英文 | 中文 | 字号 |
|---|---|---|---|
| 正文（英文段 / 中文段 / 词汇 / 句子 / 时间戳） | Calibri | 仿宋 | 12pt（小四） |
| 文档大标题 | 黑体 | 黑体 | 22pt 加粗居中 |
| 各级小标题（一、/ 二、/ ①…） | Calibri | 仿宋 | 16pt 加粗 |

### 排版

- 行距 1.4，段后留 6pt
- 正文段（英文 + 中文）**首行缩进 2 字符**；标题、时间戳、头部行**顶格**
- 每个区块（时间戳 + 英文 + 中文）后面**空一行**再接下个区块
- **词汇表里收录过的词和词组，在正文里加粗**
- 中文引号用**英文直引号 `"`**，不用中文弯引号 `“ ”`
- 页脚居中页码

### 明确不做

- ❌ 不加「处理说明」
- ❌ 不加「全篇大意」小标题
- ❌ 不加「怎么用这份稿子」
- ❌ 不加末尾版权行
- ❌ 不做 Excel 复习版（只要 Word）

---

## 四、想改规范，怎么说

| 想改什么 | 这样说 |
|---|---|
| 换字体 | "中文改成宋体""英文换成 Times New Roman" |
| 换字号 | "正文改成 11pt""标题再大一点" |
| 改缩进 | "取消首行缩进""缩进改成 4 个字符" |
| 改分段长度 | "每段短一点，60–80 词" |
| 换词汇分组 | "词汇按'日常口语 / 学术表达'分两组" |
| 词汇条数 | "词汇多给一些，50 条左右" |
| 加/减章节 | "加上'怎么用这份稿子'""把重点词汇那节也去掉" |
| **要更快** | "太慢了，词表少给点""提示只写最难的几条" —— 见下节 |
| 改文件名 | "文件名只留标题""日期放后面""用短横线隔开" |
| 改存放位置 | "存到 D:\学习资料\" |

改完之后，如果满意，可以直接说 **"把刚才的改动更新到技能里"**，
下次生成就按新规范走，不用每回都交代。

---

## 五、技能里都装了什么

```
youtube-subtitle-study-doc/
├── SKILL.md                    主流程（技能被触发时读这个）
├── 使用说明书.md                 ← 你正在看的
├── references/
│   ├── spec.md                 成品规范全字段（结构/字体/字号/缩进/间距/词表/引号）
│   ├── proofreading.md         ASR / 机翻错误速查信号（11 类，带真实例子）
│   ├── docx-pipeline.md        生成 docx 的命令序列与参数模板
│   └── docx-pitfalls.md        踩坑清单（editor SDK + WPS）
└── scripts/
    ├── scan_subtitles.py       扫字幕：块数、词数、建议段数、噪声块
    ├── bold_terms.py           按词表给正文词条批量加粗
    ├── build_docx.py           ⚡ 一键流水线：中间稿 → 成品 docx（含补丁 + 自检）
    ├── apply_fonts.py          设基准字体 + 剥离段落直设字体
    ├── fix_title_and_labels.py 补大标题黑体与居中、去掉头部标签的加粗
    ├── fix_paraid.py           去重复 paraId（WPS 副作用）
    ├── set_indent.py           逐段设首行缩进（顺带修好"自闭合空段"）
    └── verify_docx.py          交付前全项校验
```

### 脚本也能单独用

```bash
PY="$HOME/.workbuddy-ai/binaries/python/versions/3.13.12/python.exe"
S="$HOME/.workbuddy-ai/skills/youtube-subtitle-study-doc"
export PYTHONIOENCODING=utf-8          # 中文路径必备

# ⚡ 一条命令出成品（最常用：中间稿 → docx，含 4 个补丁）
"$PY" -B "$S/scripts/build_docx.py" "E:/.../中间稿.md" "E:/.../2026-09-24 标题.docx"

# 先看看字幕规模
"$PY" "$S/scripts/scan_subtitles.py" "$HOME/Downloads/xxx.txt"

# 成品自检（交付前跑一遍）
"$PY" "$S/scripts/verify_docx.py" "E:/EnglishStudy/YouTube英语字幕/2026-09-23 The Reality of Change.docx" \
      --en Calibri --cn 仿宋 --size 12 --expect-blocks 74

# 只换字体，不动内容
"$PY" "$S/scripts/apply_fonts.py" "E:/.../2026-09-23 The Reality of Change.docx" --en Calibri --cn 仿宋 --size 12

# 补大标题黑体 + 居中、去掉头部标签的加粗（头部行数自动探测，不用手填）
"$PY" "$S/scripts/fix_title_and_labels.py" "E:/.../2026-09-24 get up and keep going.docx"

# 逐段设首行缩进（正文 2 字符，标题/时间戳/空段顶格）
"$PY" "$S/scripts/set_indent.py" "E:/.../2026-09-24 get up and keep going.docx"
```

---

## 六、几个已知的坑（技能里已经自动规避）

1. **WPS 不认文档默认样式里的缩进** —— 所以首行缩进是**逐段写死**的，
   在 WPS 和 Word 里看起来一样。这不是啰嗦，是必须。
2. **中英字体没法用接口一次设好** —— 接口只能设一个字体名，会同时管英文和中文。
   所以技能是直接改文档内部 XML，才能做到「英文 Calibri + 中文仿宋」。
3. **WPS 保存会重写样式表** —— 技能会把基准字体同时写进两个地方，
   避免你在 WPS 里存一次就变回默认字体。
4. **插空行会把上一段的格式"搬走"** —— 所以技能的顺序是：先插完所有空行，最后再统一设缩进。
5. **WPS 可能留下重复的段落 ID** —— Word 打开会提示"修复文档"，技能会自动清掉。
6. **markdown 导入有四个"静默坑"** —— `# 大标题` 不给中文字体（会回落成仿宋）、
   同一个大标题也**不给居中**（会变成左对齐）、`**标签：**` 会落成加粗（规范要求标签不加粗）、
   相邻加粗 `**a** **b**` 会吞掉中间空格。
   四个都是**不报错但结果不对**，所以技能最后会专门跑脚本补掉。
7. **你开着 WPS 时那个文件是独占的** —— 这时改不进去，只能先把改好的版本存成 `.new`。
   技能会看目录里有没有 `~$` 开头的属主文件来判断占用
   （注意它的名字是 `~$` + 原文件名**去掉前 2 个字符**）。
   **如果它请你关掉 WPS，记得选「不保存」** —— 你那边显示的是旧内容，保存会盖掉新改动。
8. **插空行建出来的段落是"自闭合"的** —— 它没有段落属性容器，直接往上写缩进会把文档写坏。
   技能用 `set_indent.py` 统一展开再补缩进，所以这类文件**别用别的工具手动改**。
9. **编辑器接口偶尔会"刚建好就说文档没打开"** —— 这是瞬时抽风，**等一下重试就好**，
   技能会自动重试，不用你管。

---

## 七、为什么现在出稿只要几秒

老流程是**一步一步敲命令**：每一步都要重新启动一次 Python（每次约 1 秒，
真正干活只占 0.15 秒），一份稿子下来 30 多次调用 —— 光插 26 个空行就要 31 秒，
再加上每次思考、往返的开销，**实测一份能拖到 12 分钟**。

现在 `build_docx.py` 把整条链子**串在同一个进程里跑**：进程只启一次、
接口地址只探一次、中间不用再思考。**实测 5–6 秒出成品**，
而且它会先自检中间稿（引号、字数、区块数不对就直接停，不去碰编辑器），
成品和你手工一步步做出来的**逐字一样**（14 项指标 + 两万四千多字符全同）。

---

## 八、嫌慢的时候，砍哪里（2026-09-24 定）

**先说结论：慢不在程序上，在"写多少字"上。**

拿一份 15 分钟的稿子量过（223 段）：

| 章节 | 段数 | 占段落量 | 贡献的中文字 |
|---|---|---|---|
| 头部 + 全篇大意 | 4 | 2% | 252 |
| 一、分段精读 | 89 | 40% | 2879 |
| **二、重点词汇** | 104 | **47%** | 874 |
| **三、重点句子**（已删） | 26 | **12%** | 469 |

**58% 的段落来自"词汇 + 句子"，却只贡献 17% 的内容。** 所以提速只能从这里下刀 ——
「分段精读」是中英对照的命根子，动不了。

**已经砍掉的三刀（2026-09-24）：**

1. **「三、重点句子」整节删除** —— 那 8 句正文里都有、还带译文，唯一有价值的"看点"
   并进了词条的 `提示：`。省 12% 段落量，**零损失**。
2. **`提示：` 只给 6–8 条有坑的写** —— 不再每条都写废话提示（"搭配介词 with"这种）。
3. **词表 33 条 → 20–22 条（4 组 → 3 组）** —— 只挑正文里真难、真高频的。

**明确不值得动的：**

- ❌ **`build_docx.py` 那套管道**：现在 5–6 秒跑完，占整体耗时**不到 1%**。优化它等于白忙。
- ❌ **空段 / 时间戳 / 页脚页码 / 三行头部**：全自动生成，0 生成成本，已经极简。
- ❌ **中文译文**：唯一的大头（近 2900 字），删了就不是中英对照稿了。

**还能再砍的（如果还嫌慢，说一声就动手）：**

- 词表再降到 15 条左右、提示只留 4–5 条
- 「全篇大意」从 200–250 字压到 100–150 字

---

## 九、一句话总结

**给 txt，拿 Word。规范已经定死，你说改哪儿它改哪儿。**
