---
name: youtube-subtitle-study-doc
description: 把 YouTube 自动字幕 txt（时间戳 / 英文 ASR / 中文机翻）整理成中英对照的英语精读 Word 稿。含语义分段、ASR 与机翻错误校对、全篇大意、重点词汇（3 组 20–22 条、带原文例句、只给难词写提示），以及固定的排版规范（英文 Calibri / 中文仿宋 / 12pt、首行缩进 2 字符、区块间空行、词汇在正文加粗、可点超链接、居中页码）。当用户提供 YouTube 字幕 txt、提到"字幕整理""精读稿""中英对照""学习稿""校对机翻""ASR 错误"，或要把字幕做成 Word 学习材料时使用。
agent_created: true
---

# YouTube 字幕 → 中英对照精读 Word 稿

## 用途

把 YouTube 自动生成的字幕 txt（典型 4 行一块：时间戳 / 英文 ASR / 中文机翻 / 空行）
整理成一份可直接打印或对着视频精读的中英对照 Word 稿。

核心价值在两件事：**修掉 ASR 与机翻的错误**，以及**按固定规范产出排版统一的成品**。
规范是用户逐轮打磨定下来的，**不要自由发挥**。

## 触发场景

- 用户给一个字幕 txt，说要"整理成学习材料 / 精读稿 / 中英对照"
- 用户说"机翻哪里错了""帮我改改 ASR 识别错误"
- 用户要把之前做好的稿子按新规范重排（改字体、改缩进、改结构）

## 🔴 开工前必问：原视频链接（2026-09-24 用户二次改口径）

**被触发后的第一件事，就是检查用户有没有提供原视频链接。**

| 情况 | 怎么做 |
|---|---|
| **给了链接** | 直接开工，头部走 **3 段**（标题 / 原视频链接 / 整理日期） |
| **没给链接** | **先停下来问，不要急着跑**：「没看到原视频链接 —— 要提供吗？没有的话我就按无链接版本做（头部只留标题 + 整理日期）」 |
| 用户答**没有** | 按 **2 段**头部跑；**不要上网去找** |
| 用户**补发链接**（含事后补发） | 只加一行 `原视频链接：`，其余不动 |

🚫 **「频道」「视频时长」已废弃，永远不要写、也不要上网查**
（2026-09-24 用户原话：「以后都不需要整理'频道'，'视频时长'了」—— 嫌查这些太耗时间）。
老稿子里已经有的不用回头改，新稿一律不写。

⚠️ **这一问必须问** —— 既不要自作主张按无链接跑，也不要自作主张上网查元信息。

## 输入与输出

- **输入**：YouTube 自动字幕 txt（可能来自 `Downloads`，或用户直接粘贴）
- **输出**：单个 `.docx`，**文件名 = `当天日期 + 空格 + 视频标题`**
  （如 `2026-09-23 The Reality of Change.docx`），存到用户指定目录
  （本项目惯例：`E:\EnglishStudy\YouTube英语字幕\`）
  - 日期取**生成当天**，格式 `YYYY-MM-DD`
  - **标题来源（按优先级，2026-09-24 定）**：① 用户给的 **txt 文件名去扩展名**（默认口径）
    → ② 没有文件名时**按英文内容自拟**
  - 标题与文档里的大标题**逐字一致**
  - ⚠️ 标题里若含 Windows 非法字符 `\ / : * ? " < > |`，**替换成 `-`**（如
    `Why It's Hard: A Story` → `Why It's Hard- A Story`）；结尾的 `.` 和空格也去掉
  - 标题过长时**不截断**（保留完整标题便于辨认），若用户嫌长再改

## 成品规范（摘要）

详细字段、字号、缩进值见 `references/spec.md`，开工前读一遍。

**结构顺序**
1. 标题行（**条件性**，看用户有没有给原视频链接）：
   - **给了链接 → 3 段**：标题（居中）/ `原视频链接：`（独立成行 + 超链接）/ `整理日期：…`
   - **没给链接 → 2 段**：标题（居中）/ `整理日期：…`
     （**链接行也不写**，也不要上网找）
   - 🚫 一律**不写** `频道：` / `视频时长：`（已废弃）
2. 全篇大意正文（**不写小标题**，200–250 字）
3. `一、分段精读`：`[起始时间戳]` + 英文段 + 中文段 + **空段**
4. `二、重点词汇`：**3 组 × 7–8 条 = 全文 20–22 条**；每条固定 2 行
   （`**N. term**　词性　释义` + `例：… — 中文`）；
   **只有 6–8 条"有坑"的**才加第 3 行 `提示：`（提示可兼写句式看点）
5. ~~`三、重点句子`~~ —— **2026-09-24 整节删除**（与正文重复，占 12% 段落量；
   有价值的"看点"并进词条 `提示：`）。文档到「二、重点词汇」结束。
6. 页脚居中页码

**排版**
- 正文：英文 `Calibri` / 中文 `仿宋` / `12pt`（小四）
- 大标题：黑体 22pt 加粗居中；各级小标题：16pt 加粗
- 行距 1.4，段后 6pt
- 正文段（英文 + 中文）首行缩进 2 字符；标题 / 时间戳 / 头部行 / 空段显式归零
- 词汇表里出现过的词和词组，**在正文中加粗**
- 中文引号用 **ASCII 直引号 `"`**，不用中文弯引号 `“ ”`（2026-09-24 定）

**明确不要的**：处理说明、「全篇大意」小标题、「怎么用这份稿子」、末尾版权行、Excel 复习版。

## 工作流

### 阶段 1 · 读素材、定标题与切分

0. **先确认原视频链接**（见上「🔴 开工前必问」）：
   给了 → 3 段头部，直接开工；**没给 → 先停下问用户**，
   等他答"没有"或补发链接，再决定头部形态。
   顺便定标题：默认取 **txt 文件名去扩展名**；没有文件名就**按英文内容自拟**。
   （**不要去查频道名/时长** —— 已废弃，查了纯属浪费时间。）
1. 读完整 txt，统计字数与字幕块数（`scripts/scan_subtitles.py` 可先跑一遍看规模）
2. 按**语义**切分，不按时间戳切。目标 **每段 90–130 词**（41 分钟 / 7000 词 → 约 70 段）；
   **短素材允许 80–90 词/段**（不足 1500 词或语义天然碎时，宁可段落略短，也不硬拼），下限不低于 80 词
3. 只保留**每段起始**的时间戳，格式 `[MM:SS]`

### 阶段 2 · 校对

对照 `references/proofreading.md` 里的速查信号逐类扫。原则：

- **只修识别错误，不"提升"说话人的英语**：保留口语原貌（you know / like / 自我修正 / 修辞性重复），
  只删无意义结巴（"I I"、"for for"）
- 拿不准的残缺句**不瞎补**，标 `[听不清]`
- 中文以"读得懂"为准，长句按中文习惯拆开
- **中文只保留修正后的版本，不留改动痕迹**（不写"原文/修正后"）
- 涉及抑郁、焦虑、亲人离世等内容如实翻译，不淡化也不加戏

### 阶段 3 · 写 Markdown 中间稿

先产出纯 Markdown（后续喂给编辑器导入），包含：头部 → 全篇大意 → 分段精读 → 重点词汇。
（**没有「重点句子」了** —— 2026-09-24 删。）

- 词汇条目里的词条用 `**加粗**`
- **词表组标题写成三级标题 `### ① …`**（**`###` 不能省**，否则会被当成正文段，
  拿不到 16pt 加粗 + 缩进归零；详见 `spec.md` §4）
- 正文中**凡是词汇表收录过的词和词组，也用 `**加粗**`**（用 `scripts/bold_terms.py` 批量处理）
  - ⚠️ **正文加粗是独立一步，`build_docx.py` 不含它**。正确顺序：
    `中间稿 → bold_terms.py --terms <词表> -o <加粗版.md> → build_docx.py <加粗版.md> <out.docx>`
  - 词表文件**一行一个正则片段**，要匹配**正文里的实际形态**而非词表原形
    （如 `rush\w* into commitment`、`cram\w* for the exam`、`lean\w* into`、`subsid\w*`、
    `pac\w* (our|your)sel\w*`、`on hold`）
  - 漏了这步的**信号**：成品「加粗 run 数」只有 40 上下（正常应在 80 以上）
- 超链接写 `[文字](url)`
- **不要用引用块 `> `**（导入后每行会被拆成独立段落，见 pitfalls）
- 🕐 **提速要点**：词表只挑**正文里真难、真高频**的 20–22 条；
  `提示：` 只给 6–8 条有坑的写。**别为了凑数写废话提示** —— 那正是最费时又最没用的一步。

### 阶段 4 · 生成 docx

**首选：一条命令跑完。** 把「建文档 → 写内容 → 插空段 → 页码 → 保存 → 4 个补丁」串进一个进程：

```bash
PY="$HOME/.workbuddy-ai/binaries/python/versions/3.13.12/python.exe"
S="$HOME/.workbuddy-ai/skills/youtube-subtitle-study-doc/scripts"
export PYTHONIOENCODING=utf-8
"$PY" -B "$S/build_docx.py" "<中间稿.md>" "<输出.docx>"
```

实测 **5–6 秒**（26 区块）/ 第二份 54 区块也就 6 秒出头；拆成手工调用则要 30+ 次
`edsdk.py` 往返、**每次进程启动 1.0 秒**（真正通信只有 0.15 秒），光 26 次插空段就 31 秒，
外加每一轮 Agent 的推理开销 —— 实测一份稿子能到 12 分钟。

参数：`--no-patch`（只生成不打补丁）、`--keep-open`（不关编辑器实例）、
`--skip-check`（跳过中间稿自检）、`-v`（打印重试/清理细节）。

脚本自带**中间稿自检**，不合格直接停、**不碰 SDK**：`# 大标题` 存在、区块数、
两个二级标题存在且顺序对、全篇大意 200–250 中文字、无中文弯引号、无相邻加粗、每行 `**` 成对。

**它内部做了什么**（要手工重做时照这个顺序）：

1. `clean_pool`：清掉残留的 `new_doc_*` 实例（前次失败留下的会引发竞态）
2. `create_doc`（**返回纯文本**，`file_id` 要正则抠，不是 JSON）
3. `doc_set_document_style` 设字体/字号/行距/段间距
4. `doc_insert_markdown` 写入全文（`markdown` 传 `file://<绝对路径>`）
5. `doc_resolve_document_structure`（**`limit=0` 才不分页**，默认 150 会截断）→ 定位中文段
6. `doc_insert_paragraph_with_text` 插区块间空段（**从后往前**，前面坐标才不漂移）
   - ⚠️ 建出来的是**自闭合 `<w:p .../>`**，没有 `<w:pPr>`；
     **别手写 XML 去补**，交给第 8 步 ④ 的 `set_indent.py`
7. `doc_set_page_number` → `save_file`（页码是**覆盖式**写页脚，必须放最后设）
8. **`close_file --force` 后**用 zipfile 改 XML，**按这个顺序**：
   ① `apply_fonts.py`（中文字体 + Normal 样式补 rPr）
   ② `fix_title_and_labels.py`（大标题补黑体 **+ 补居中 `<w:jc>`**；头部标签取消加粗；
      **头部标签行数自动探测**，2 段头部时只有「整理日期」1 个标签）
   ③ `fix_paraid.py`（去重复 paraId）
   ④ `set_indent.py`（**逐段设首行缩进**：正文 2 字符 / 标题·时间戳·头部行·空段归零）

命令模板与字段细节见 `references/docx-pipeline.md`。

**顺序铁律**
- **首行缩进放最后**（第 6 步 ④）。插空段会把源段落的直接格式搬走，先设等于白设。
- **SDK 操作全部做完再打 XML 补丁** —— `save_file` 会让 WPS 重写 `styles.xml`。
- 缩进**不要**用 `doc_modify_paragraph` 做：`doc_resolve_document_structure` **有分页**
  （实测 `limit:150 / total_nodes:203 / has_more:true`），长文档拿不全坐标；
  XML 层（`set_indent.py`）一次就能遍历全部段落，还能顺手展开自闭合空段。
- 这四个脚本**幂等**，改完脚本要拿成品副本重跑一遍回归（`set_indent.py` 应报「改写段数 0」）。

**markdown 的四个坑（都会静默出错，必须事后补）**
- `# 大标题` **不给中文字体** → 要补 `黑体`（否则回落成仿宋）
- `# 大标题` **不给 `<w:jc>`** → 要补 `<w:jc w:val="center"/>`（否则大标题左对齐）
- `**标签：**` 落成的是**加粗**，规范要求标签不加粗 → 要改成 `<w:b w:val="0"/>`
- 相邻加粗 `**a** **b**` 会**吞掉中间空格** → 词表加粗时就合并成 `**a b**`

### 阶段 5 · 校验与交付

跑 `scripts/verify_docx.py`，逐项核对：段落数 / 标题 / **大标题居中** / 超链接 / 版权行 /
各类缩进计数 / 加粗计数 / 字体名统计 / 字号 / paraId 重复 / zip 完整性 / XML 良构。
全绿再 `present_files`。
**头部行数（2 段 / 3 段）脚本会自动探测**；没写「原视频链接」时超链接检查自动跳过，不算失败；
写了「频道」/「视频时长」会被标成 ❌。
另外**人工抽查**：
① 正文里有没有两个单词粘在一起（相邻加粗副作用）；
② 「全篇大意」中文字数是否落在 200–250；
③ **段落数与 paraId 数是否相等** —— 差一截说明漏了自闭合空段（见铁律 17）。

**交付文件名 = `当天日期 + 空格 + 视频标题`**（如 `2026-09-23 The Reality of Change.docx`）：
- 日期用生成当天的 `YYYY-MM-DD`，标题与文档大标题**逐字一致**
  （**标题来源见上：txt 文件名去扩展名优先，无文件名再按英文内容自拟**）
- 标题含 `\ / : * ? " < > |` 时替换为 `-`；结尾 `.` 与空格去掉
- 若同名文件已存在（同一天做了两个视频），**先确认再覆盖**，不要静默覆盖用户的文件

## 铁律（违反会导致返工）

1. **缩进在插段之后设**。`doc_insert_paragraph_with_text` 会把源段落的直接段落格式搬给新空段。
2. **中英分设字体必须改 XML**。SDK 的 `font_family` 只能写一个名字，会同时落到 ascii/hAnsi/eastAsia。
3. **基准字体要同时写 `docDefaults` 和 `Normal` 样式**。WPS 保存会把 docDefaults 的内容搬进 Normal。
4. **WPS 不认 `docDefaults` 里的 `<w:ind>`**。首行缩进必须逐段显式设。
5. **剥离直设格式时跳过标题段**（判据：段落里有没有 `<w:outlineLvl>`），否则会把标题字号抹掉。
6. **校验脚本写成 `.py` 文件再跑**。`python -c "..."` 里 bash 双引号会吃掉反斜杠，正则匹配数会变成 0。
7. **用户手改过的文档，以 XML 实际值为准**。用户的文字描述可能与文件不符（本技能就踩过一次：
   用户说"宋体"，文件里实际是"仿宋"）。**发现差异先问，别照话面猜。**
8. **`create_doc` 返回纯文本**，`file_id` 要正则抠（`file_id=(\S+?),`）。
9. **大标题黑体 + 头部标签取消加粗必须事后改 XML**，markdown 表达不出来。
10. **相邻加粗会吞空格**：`**a** **b**` → `ab`，词表加粗时就要合并成 `**a b**`。
11. **「全篇大意 200–250 字」按中文字数量**，别把英文单词算进去。
12. **字幕 txt 可能只有英文 ASR**（无中文机翻），中文全部自己译即可。
13. **头部只有三行可选：标题 / `原视频链接：` / `整理日期：`**。
    没给链接就不写链接行，头部只剩标题 + 整理日期。
    🚫 **「频道」「视频时长」永远不写、也不上网查**（2026-09-24 用户二次改口径，嫌查这些太耗时间）。
    标题默认取 **txt 文件名（去扩展名）**，无文件名再按英文内容自拟。
    **别为了"看起来完整"去网上补元信息** —— 用户明确不要。
14. 🕐 **用户把"速度"排第一时，砍的永远是「二、重点词汇」，不是「分段精读」**。
    文档只有两节：`一、分段精读` + `二、重点词汇`（**没有「三、重点句子」**，2026-09-24 删）。
    词表 **3 组 × 7–8 条 = 20–22 条**；`提示：` **只给 6–8 条有坑的写**。
    ⚠️ **管道（`build_docx.py` + 4 个补丁）只占整体耗时不到 1%**，优化它没用 ——
    实测数据：223 段里 **58% 的段落来自"词汇 + 句子"，却只贡献 17% 的内容量**。
    提速只能从"少写"下手。
15. **中文引号用 ASCII 直引号 `"`**（U+0022），**不要用弯引号 `“ ”`**（U+201C/U+201D）。
    写 markdown 中间稿时就用直引号，别等生成后再替换。
    （第一份成品里其实没出现过中文引号，别拿它当"弯 / 直"的对照物 —— 判据就是"用直引号"。）
16. **改已有 docx 前先探占用**。WPS 属主文件是 `~$` + **去掉前 2 个字符**的原名
    （`2026-09-24 x.docx` → `~$26-09-24 x.docx`），按完整名探会漏判。
    被占用就把改好的版本存成 `.new`，让用户关掉 WPS（**提醒选「不保存」**）后再覆盖。
    查进程用 `tasklist | grep -i wps` —— **本机 PowerShell 工具的输出会被吞**，不可靠。
17. **`doc_insert_paragraph_with_text(text="")` 建出的空段是自闭合 `<w:p .../>`**（没有 `<w:pPr>`）。
    段落正则一律写 **`<w:p\b[^>]*/>|<w:p[ >].*?</w:p>`** —— 少前半段会漏掉空段、
    把「段落数」数少，往它后面插 `<w:pPr>` 还会写出畸形结构（文档末尾那段最容易中招）。
    别自己手写，交给 `set_indent.py`。
    **自查信号：`fix_paraid.py` 报的 paraId 数 ≠ 段落正则数到的段数，差的就是自闭合空段。**
18. **`# 大标题` 不给 `<w:jc>`** → 大标题会左对齐（第二份没暴露，因为当时手动设过）。
    要补 `<w:jc w:val="center"/>`，位置在 `<w:ind>` 之后、`<w:outlineLvl>` 之前
    （OOXML 的 `pPr` 子元素有顺序要求）。`fix_title_and_labels.py` 已内置。
19. **首行缩进用 `set_indent.py` 在 XML 层做**，别用 `doc_modify_paragraph`：
    后者要先 `doc_resolve_document_structure` 拿坐标，而它**有分页**（`limit:150`），
    长文档拿不全；XML 层一次遍历全部段落，还能顺手展开自闭合空段。
20. **写自动化脚本时，`edsdk._rpc()` 的错误会直接 `sys.exit(1)` 杀掉你的进程**。
    `_die()` 一遇 JSON-RPC 错误就退出（同时往 stdout/stderr 各打两行），
    所以「捕获返回值再判断」的兜底分支会变成死代码。要这样包：
    ```python
    with contextlib.redirect_stdout(buf), contextlib.redirect_stderr(err):
        r = edsdk._rpc('tools/call', {'name': tool, 'arguments': args})
    ```
    配 `except SystemExit` 把失败降级成字符串。`build_docx.py` 的 `make_caller()` 已封装。
21. **`create_doc` 之后立刻 `doc_set_document_style` 可能撞竞态**
    （报 `SetDocDefaults failed: document is not opened`，但 `create_doc` 明明成功了）。
    这是**瞬时**错误，**重试就好**（实测 3 次里 2 次第 1 发就失败，隔 0.3 秒再发必成功）。
    重试前建议先 `clean_pool` 清掉前次失败残留的 `new_doc_*` 实例。

完整坑清单见 `references/docx-pitfalls.md`。

## 脚本

| 脚本 | 用途 | 跑的时候 |
|---|---|---|
| `scripts/scan_subtitles.py` | 扫字幕 txt：块数、词数、建议分段数、异常块 | 阶段 1 |
| `scripts/bold_terms.py` | 按词表给正文里的词条批量加 `**加粗**`（自动合并相邻加粗，防空格被吞） | 阶段 3 |
| `scripts/build_docx.py` | **一键流水线**：自检中间稿 → 建文档 → 写内容 → 插空段 → 页码 → 保存 → 4 个补丁，同进程完成（实测 5–6 秒） | 阶段 4（**首选**） |
| `scripts/apply_fonts.py` | 改 docx 基准字体（英文/中文/字号）+ 剥离非标题段落的直设字体；`Normal` 样式缺 `rPr` 时自动补 | 收尾 ① |
| `scripts/fix_title_and_labels.py` | 大标题补**黑体** + 补**居中 `<w:jc>`** + 头部标签取消加粗（markdown 做不到的三件事；头部标签行数自动探测） | 收尾 ② |
| `scripts/fix_paraid.py` | 去掉重复的 `w14:paraId`（WPS 保存后的常见副作用） | 收尾 ③ |
| `scripts/set_indent.py` | **逐段设首行缩进**（正文 2 字符 / 标题·时间戳·头部行·空段归零）；顺带把**自闭合空段**展开 | 收尾 ④（**最后**） |
| `scripts/verify_docx.py` | 成品全项校验（头部行数自动探测；无链接时自动跳过超链接检查；会提示自闭合空段残留） | 交付前 |

脚本都用托管 Python 跑：
`%USERPROFILE%\.workbuddy-ai\binaries\python\versions\3.13.12\python.exe`，
中文路径下先设 `PYTHONIOENCODING=utf-8`。

## 参考文档

| 文档 | 内容 |
|---|---|
| `references/spec.md` | 成品规范全字段（结构、字体、字号、缩进、间距、词表写法） |
| `references/proofreading.md` | ASR / 机翻错误速查信号与实例 |
| `references/docx-pipeline.md` | edsdk.py 命令序列与参数模板 |
| `references/docx-pitfalls.md` | editor SDK + WPS 的完整踩坑清单 |
| `使用说明书.md` | 给用户看的操作说明 |

## 维护本技能

🔴 **只在用户明确说「更新到技能里」时才改本技能的文件**（2026-09-24 用户定）。

- 用户改了规范 ≠ 让你改技能。**先只做交付物**，等他那句「更新技能」再同步。
- 发现技能里有明显错误（错字、错工具名、过时信息）时，**先告诉他**，不要擅自改。
- 理由：他不想在自己没要求的时候，技能文件被悄悄改动。

用户说了「更新到技能里」之后，**要同步改这几处**（漏一处下次就按旧规范跑）：

1. `references/spec.md` —— 规范的全字段，这是源头
2. `SKILL.md` 的「成品规范（摘要）」+「输入与输出」
3. `使用说明书.md` —— 给用户看的那份
4. 涉及的脚本默认值（如 `verify_docx.py` 的 `--en/--cn/--size`、
   `set_indent.py` 的 `--size`、`build_docx.py` 的 `--sdk-dir`；
   头部行数相关的 `--header-lines` 与 `--labels` **已改为自动探测**，一般不用动）
5. `references/docx-pitfalls.md` —— 本轮新踩的坑
6. `references/docx-pipeline.md` —— 命令序列 / 一键入口有变时

改完校验 + 打包：

```bash
PY="$HOME/.workbuddy-ai/binaries/python/versions/3.13.12/python.exe"
SC="$HOME/.workbuddy-ai/plugins/cache/workbuddy-builtin/skill-skill-creator/<版本号>"
S="$HOME/.workbuddy-ai/skills/youtube-subtitle-study-doc"
export PYTHONIOENCODING=utf-8
"$PY" "$SC/scripts/quick_validate.py" "$S"
"$PY" "$SC/scripts/package_skill.py" "$S" "<输出目录>"
```
